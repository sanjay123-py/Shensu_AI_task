## Create .env file inside the cloned folder before cloing and execution of the code
## Inside .env file create a variable **MONGO_DB_URL** and assign it with your mongodb url
`
MONGO_DB_URL = 'URL'
`
